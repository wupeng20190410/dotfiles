"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.logger = void 0;
const tslib_1 = require("tslib");
const chalk_1 = (0, tslib_1.__importDefault)(require("chalk"));
const types_1 = require("../types");
class Logger {
    constructor() {
        this.isNode = this.getPlatform() === types_1.EPlatform.node;
    }
    error(info) {
        const output = this.formatOutput(info);
        console.error(this.isNode ? chalk_1.default.red(output) : output);
    }
    log(info) {
        const output = this.formatOutput(info);
        console.log(this.isNode ? chalk_1.default.gray(output) : output);
    }
    warn(info) {
        const output = this.formatOutput(info);
        console.warn(this.isNode ? chalk_1.default.yellow(output) : output);
    }
    getPlatform() {
        if (typeof process !== 'undefined' && Object.prototype.toString.call(process) === '[object process]') {
            return types_1.EPlatform.node;
        }
        return types_1.EPlatform.web;
    }
    formatOutput(output) {
        return `[i18n] ${output}`;
    }
}
// 单例模式
exports.logger = new Logger();
